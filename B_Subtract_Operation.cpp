#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    ll t = 1;
    cin >> t;
    while(t--){
        int n , k;
        cin >> n >> k;
        int arr[n];
        unordered_map
        for(int i = 0; i < n ; i++) cin >> arr[i];
    }
    return 0;
}
